# Active Tasks

- Configure GitHub remote and branch promotion after reviewing what should be committed.
- Confirm whether bundled `chromium/` should be tracked or regenerated during build setup.
- Manually QA attached PDF size table against `knowledge-base.js` before any future size-table changes.
- Add parser/search regression fixtures for Stage 3 examples.
